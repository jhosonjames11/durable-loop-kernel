# LoopGraph Supervisor 项目说明

## 项目定位

**LoopGraph Supervisor** 是一个 **DSH-first、Harness-neutral、可持久恢复** 的 Agent Loop 监督器。

它将 Agent 执行、独立验证、循环重试、人工审批（HITL）、版本晋级与回滚，统一为可观察、可暂停、可恢复、可审计的持久化状态机。

本压缩包是源代码交付包，基于 Git 提交：

```text
d7e365225ce5a64791b745de70bc49756605a646
```

依赖目录、构建产物、Git 元数据及本地运行数据未被打包；安装依赖后即可构建和测试。

## 架构决策

项目采用“**独立 TypeScript 核心 + 外部 DeepSeek Harness（DSH）Cordis 插件适配器**”的方式，而不是 fork DSH：

- `packages/core`：不依赖 DSH；包含事件协议、纯 Reducer、状态机、监督器和中立 ports。
- `packages/storage`：SQLite append-only event journal、checkpoint/artifact 及恢复边界。
- `packages/dsh-adapter`：基于 DSH/Cordis 公开扩展点，将 DSH 生命周期与审批映射到核心。
- `packages/demo`：真实 SQLite 与文件 artifact store 的 mock-harness 端到端场景。
- `packages/cli`：`inspect`、`pause`、`resume`、`promote`、`rollback` 与 `demo` 运维命令。

因此，DSH 是一等集成目标，同时未来也可以为其他 Harness 增加 adapter，而不改变核心状态机。

## 关键能力

1. **可观察**：执行、验证、HITL、checkpoint、晋级、回滚、暂停与恢复均由不可变事件记录。
2. **可恢复**：SQLite journal、revision CAS、lease fencing、checkpoint 与 artifact manifest 支持进程重启后的保守恢复。
3. **安全暂停**：仅在安全边界暂停；对于未完成的外部模型或工具调用，恢复时进入 `PAUSED_RECOVERED`，不会静默重放。
4. **独立验证与 HITL**：晋级要求持久化验证证据；配置 HITL 时，只有允许的审批结果才能放行，其他结果 fail-closed。
5. **安全晋级与回滚**：仅可从已验证的 checkpoint 晋级；回滚追加补偿事件，绝不重写历史。
6. **并发与篡改防护**：journal 包含幂等键、CAS revision、fencing token、canonical JSON 回读校验与受限 artifact publication。
7. **DSH-first 集成**：adapter 仅使用公开的 `ctx.agents`、`agent/pre-step`、`agent/turn-stopping`、`ctx.approval.request` seam，不依赖具体 agent loop 实现。

## 快速开始

### 环境

- Node.js 22+（建议）
- npm
- 运行真实 DSH 集成测试时：一个已安装依赖的 DeepSeek Harness checkout

### 安装、构建与基础验证

在解压后的项目根目录执行：

```sh
npm install
npm run build
npm test
npm run typecheck
npm run lint
```

### Durable mock-harness 端到端测试

```sh
npm run test:demo
```

该场景使用真实 SQLite 和 artifact storage，覆盖失败执行、验证、HITL、版本晋级、崩溃恢复、显式恢复和回滚。

### 真实 DSH / Cordis 集成测试

```sh
DSH_CHECKOUT=/absolute/path/to/deepseek-harness npm run test:dsh
```

它验证外部 Cordis plugin 挂载、DSH 生命周期关联、审批映射与 fail-closed 行为。

### CLI 演示

```sh
npm run build
node packages/cli/dist/index.js demo
```

该 demo 使用临时目录并在结束时自行清理。

## 阅读顺序

1. `README.md`：能力边界、架构、存储与 CLI 说明。
2. `docs/research/deepseek-harness-audit.md`：DeepSeek Harness 源码审计与公开适配边界。
3. `docs/adr/001-external-adapter-not-fork.md`：选择插件适配器、而不是 fork DSH 的决策记录。
4. `docs/demo-script.md`：五分钟面试演示流程。
5. `.hermes/plans/2026-08-25-loopgraph-supervisor.md`：实现蓝图。

## 使用边界

- CLI 是控制平面：它只能查看和追加受约束的控制事件，不会伪造模型执行、验证或 HITL 审批。
- 每次 mutation 均需明确给出 `--run`、`--actor` 和最新 `--expected-revision`。
- `inspect` 仅输出有界且脱敏的状态投影，不输出 prompts、artifact 内容、原始 event payload 或敏感引用。

## 打包内容

本包只包含 Git 受版本控制的源代码、测试、文档、配置和依赖锁定文件，便于复现安装与审查；不包含：

- `node_modules/`
- TypeScript 构建产物（`dist/`）
- `.git/` 版本库元数据
- 临时 SQLite 数据库与 artifact 文件
- 本机环境变量、密钥或其他本地敏感信息
